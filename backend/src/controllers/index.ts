/**
 * Controller exports.
 * Controllers handle HTTP concerns only — parsing request, calling service, sending response.
 * Controllers are added per phase alongside their corresponding routes and services.
 */

export {};
